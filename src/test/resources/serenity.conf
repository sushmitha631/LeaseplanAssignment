environments {
  default {
    baseurl = "https://waarkoop-server.herokuapp.com"
    basepath = "/api/v1/search/demo/"
    validSchemaValidator = "schema/search_product.json"
    contentType = "application/json"
  }
  //env can be anything: dev, qa, staging, etc.
}

serenity.project.name="Search for products Test"
